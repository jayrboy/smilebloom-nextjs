# Summary ขั้นตอนการทำเว็บไซต์ Smilebloom

เอกสารนี้สรุปขั้นตอนการทำเว็บไซต์ด้วย **Next.js** และ **MongoDB** แบบกระชับ เพื่อให้นำไปจัดต่อใน Word ได้ง่าย

---

## 1. เตรียมโปรเจกต์

เริ่มจากสร้างโปรเจกต์เว็บด้วย Next.js และใช้ TypeScript เพื่อช่วยตรวจสอบโค้ดให้ถูกต้องมากขึ้น

เครื่องมือหลักที่ใช้:

- Next.js สำหรับสร้างหน้าเว็บและ API
- React สำหรับสร้างส่วนติดต่อผู้ใช้
- TypeScript สำหรับช่วยลดข้อผิดพลาดของโค้ด
- TailwindCSS สำหรับตกแต่งหน้าเว็บ
- MongoDB สำหรับเก็บข้อมูล
- Mongoose สำหรับเชื่อมต่อและจัดการข้อมูลใน MongoDB

พื้นที่ใส่รูปภาพ:



---

## 2. ติดตั้ง Dependencies

ติดตั้ง package ที่จำเป็นสำหรับโปรเจกต์ เช่น Next.js, React, Mongoose และ NextAuth

คำสั่งที่ใช้:

```bash
npm install
```

หรือถ้าใช้ pnpm:

```bash
pnpm install
```

พื้นที่ใส่รูปภาพ:



---

## 3. ตั้งค่า Environment Variables

สร้างไฟล์ `.env.local` เพื่อเก็บค่าที่ไม่ควรเขียนไว้ในโค้ดโดยตรง เช่น URL ฐานข้อมูลและ secret สำหรับระบบเข้าสู่ระบบ

ตัวอย่างค่าที่ใช้:

```env
MONGODB_URI=mongodb://localhost:27017/smilebloom
NEXTAUTH_SECRET=replace-me
NEXTAUTH_URL=http://localhost:3000
```

คำอธิบายสั้น ๆ:

- `MONGODB_URI` คือที่อยู่สำหรับเชื่อมต่อ MongoDB
- `NEXTAUTH_SECRET` คือรหัสลับของระบบ login
- `NEXTAUTH_URL` คือ URL หลักของเว็บไซต์

พื้นที่ใส่รูปภาพ:



---

## 4. เชื่อมต่อ MongoDB

สร้างไฟล์เชื่อมต่อฐานข้อมูล เพื่อให้ API และหน้าเว็บสามารถอ่าน/บันทึกข้อมูลลง MongoDB ได้

ตัวอย่างแนวคิด:

```js
import mongoose from "mongoose";

export async function connectMongoDB() {
  return mongoose.connect(process.env.MONGODB_URI);
}
```

การเชื่อมต่อฐานข้อมูลจะถูกเรียกใช้ใน API routes เช่น สมัครสมาชิก ข้อมูลเด็ก ข้อมูลฟัน และโปรไฟล์

พื้นที่ใส่รูปภาพ:



---

## 5. ออกแบบ Models

สร้าง Model เพื่อกำหนดโครงสร้างข้อมูลใน MongoDB

Models หลักของเว็บไซต์:

- `User` เก็บข้อมูลผู้ใช้
- `Child` เก็บข้อมูลเด็ก
- `Teeth` เก็บข้อมูลฟัน
- `TeethEvent` เก็บประวัติเหตุการณ์เกี่ยวกับฟัน

ตัวอย่างข้อมูลที่จัดเก็บ:

- ชื่อผู้ใช้
- ชื่อเด็กและวันเกิด
- ข้อมูลฟัน
- ประเภทเหตุการณ์ เช่น ฟันขึ้น ฟันหลุด ถอนฟัน หรือบันทึกเพิ่มเติม

พื้นที่ใส่รูปภาพ:



---

## 6. สร้าง API Routes

ใช้ API Routes ของ Next.js สำหรับรับส่งข้อมูลระหว่างหน้าเว็บกับ MongoDB

API หลักที่ใช้:

- `POST /api/register` สำหรับสมัครสมาชิก
- `GET /api/profile` สำหรับดึงข้อมูลโปรไฟล์
- `PATCH /api/profile` สำหรับแก้ไขโปรไฟล์
- `GET /api/children` สำหรับดึงข้อมูลเด็ก
- `POST /api/children` สำหรับเพิ่มข้อมูลเด็ก
- `GET /api/teeth` สำหรับดึงข้อมูลฟัน
- `GET /api/teeth-events` สำหรับดึงข้อมูลเหตุการณ์ฟัน
- `POST /api/teeth-events` สำหรับเพิ่มเหตุการณ์ฟัน

พื้นที่ใส่รูปภาพ:



---

## 7. สร้างระบบสมัครสมาชิกและเข้าสู่ระบบ

ระบบสมัครสมาชิกจะรับ username และ password จากผู้ใช้ แล้วบันทึกลง MongoDB

ระบบเข้าสู่ระบบใช้ NextAuth แบบ Credentials เพื่อตรวจสอบ username และ password ก่อนอนุญาตให้เข้าใช้งานหน้า Dashboard

ขั้นตอนโดยรวม:

1. ผู้ใช้สมัครสมาชิก
2. ระบบบันทึกข้อมูลผู้ใช้ลง MongoDB
3. ผู้ใช้เข้าสู่ระบบ
4. ระบบตรวจสอบข้อมูลและสร้าง session
5. ผู้ใช้เข้าใช้งานหน้าเว็บได้

พื้นที่ใส่รูปภาพ:



---

## 8. สร้างหน้าเว็บไซต์

สร้างหน้าหลักของเว็บไซต์ด้วย Next.js App Router

หน้าสำคัญในเว็บไซต์:

- หน้าแรก แนะนำเว็บไซต์
- หน้า Register สำหรับสมัครสมาชิก
- หน้า Login สำหรับเข้าสู่ระบบ
- หน้า Dashboard สำหรับดูภาพรวม
- หน้า Teeth สำหรับดูและบันทึกข้อมูลฟัน
- หน้า Profile สำหรับข้อมูลผู้ใช้และนัดทันตแพทย์

พื้นที่ใส่รูปภาพ:



---

## 9. จัดการข้อมูลฟันและเหตุการณ์

หน้า Teeth ใช้สำหรับดูข้อมูลลำดับการขึ้นของฟัน และบันทึกเหตุการณ์ของเด็กแต่ละคน

ตัวอย่างเหตุการณ์ที่บันทึกได้:

- `ERUPTED` ฟันขึ้น
- `SHED` ฟันน้ำนมหลุด
- `EXTRACTED` ถอนฟัน
- `NOTE` บันทึกเพิ่มเติม

ข้อมูลที่บันทึกจะถูกส่งไปยัง API แล้วเก็บลง MongoDB

พื้นที่ใส่รูปภาพ:



---

## 10. ทดสอบการทำงาน

รันเว็บไซต์ในเครื่องเพื่อทดสอบหน้าเว็บ ระบบเข้าสู่ระบบ และการเชื่อมต่อฐานข้อมูล

คำสั่งที่ใช้:

```bash
npm run dev
```

เปิดเว็บไซต์ที่:

```text
http://localhost:3000
```

สิ่งที่ควรทดสอบ:

- สมัครสมาชิกได้
- เข้าสู่ระบบได้
- เพิ่มข้อมูลเด็กได้
- บันทึกเหตุการณ์ฟันได้
- ข้อมูลถูกบันทึกลง MongoDB

พื้นที่ใส่รูปภาพ:



---

## 11. Build สำหรับใช้งานจริง

เมื่อตรวจสอบเรียบร้อยแล้ว ให้ build โปรเจกต์เพื่อนำไปใช้งานจริง

คำสั่งที่ใช้:

```bash
npm run build
npm start
```

ถ้าใช้ Docker สามารถรันผ่าน `docker compose` ได้

```bash
docker compose up --build
```

พื้นที่ใส่รูปภาพ:



---

## สรุป

เว็บไซต์ Smilebloom พัฒนาด้วย Next.js และใช้ MongoDB เป็นฐานข้อมูลหลัก โดยมีระบบสมัครสมาชิก เข้าสู่ระบบ Dashboard จัดการข้อมูลเด็ก บันทึกข้อมูลฟัน และบันทึกประวัติเหตุการณ์เกี่ยวกับฟันของเด็ก

พื้นที่ใส่รูปภาพ:



